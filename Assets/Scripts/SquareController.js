#pragma strict

import GameSparks.Api.Requests;
import GameSparks.Api.Responses;
import GameSparks.Api.Messages;
import GameSparks.Core;

var sqColor : Color;
var hasPieceNum : int;
var hasPieceDen : int;
var piece : GameObject;
var pieceScript : ColorAssign;
var ind : Array;
//private static var gameState : CreateBoard;
/*
private static var aPosHandled : boolean;
private static var iniPosSquare : SquareController;
private static var finPosSquare : SquareController;
private static var prevInd : int;
private static var prevE : int;
*/

function Start () {
	//gameState = GameObject.Find("Board").GetComponent(CreateBoard);
	//sqColor = render.color;

}

function Update () {
	//if (mes != null){
	//	Debug.Log("WTF!");
	//}
	/*
	if(mes != null){
		//Debug.Log(count);
		count++;
		if (count == 65){
			Debug.Log("All Squares Received The Message");
			count = 0;
			mes = null;
		}else{
			//Debug.Log(pos.ToString() + " received");
			handleOpponentMove(mes);
		}
	}
	//Debug.Log("Right Click");
	}
	*/
}


